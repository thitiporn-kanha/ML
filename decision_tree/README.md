# Decision Tree (template)

Place your dataset CSV in `decision_tree/data/data.csv` and set the target column name inside `main.py` or pass arguments if you modify the script.

Run:
```bash
cd decision_tree
python main.py
```

Outputs written to `decision_tree/results/` (results.json, model.joblib).
