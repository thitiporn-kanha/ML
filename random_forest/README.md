# Random Forest (template)

Place your dataset CSV in `random_forest/data/data.csv` and set the target column name inside `main.py` or update the script.

Run:
```bash
cd random_forest
python main.py
```

Outputs written to `random_forest/results/` (results.json, model.joblib).
