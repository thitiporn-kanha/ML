# KMeans Template

Usage:

- Place dataset at `kmeans/data/data.csv` with numeric features.
- Run `./main.py`.

Outputs:

- `kmeans/results/results.json` — inertia
- `kmeans/results/with_clusters.csv` — original data with cluster labels
- `kmeans/results/model.joblib` — saved KMeans model
